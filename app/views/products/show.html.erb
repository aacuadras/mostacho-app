<p id="notice"><%= notice %></p>

<%= turbo_frame_tag dom_id(@product) do %>
  <%= render @product %>
<% end %>

<%= link_to 'Edit', edit_product_path(@product) if user_signed_in? %> |
<%= link_to 'Back', products_path %>
